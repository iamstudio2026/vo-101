name: Deploy to Hosting VPS

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd ~/vo-101
            git fetch --all
            git reset --hard origin/main
            # Stop host Nginx if it's running and taking port 80
            sudo systemctl stop nginx || true
            sudo systemctl disable nginx || true
            docker-compose down
            # Create a temporary .env for the build if needed, or pass via build-args
            docker-compose build --build-arg VITE_OPENROUTER_API_KEY=${{ secrets.VITE_OPENROUTER_API_KEY }} --build-arg VITE_GEMINI_API_KEY=${{ secrets.VITE_GEMINI_API_KEY }}
            docker-compose up -d
            docker ps
            docker logs --tail 20 vo-101-app-1 || true
            docker system prune -f
