version: '3.8'

services:
  app:
    build: .
    ports:
      - "80:80"
    restart: always
    environment:
      - VITE_GEMINI_API_KEY=${VITE_GEMINI_API_KEY}
      - VITE_OPENROUTER_API_KEY=${VITE_OPENROUTER_API_KEY}
